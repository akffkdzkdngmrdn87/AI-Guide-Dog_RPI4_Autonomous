# ⚙️ Raspberry Pi 4 기반 Edge AI 하드웨어 성능 최적화 가이드

본 문서는 자원 제약이 있는 환경(Raspberry Pi 4, 4GB RAM)에서 딥러닝 기반 자율주행 시스템을 구동하기 위한 하드웨어 성능 조정(Performance Tuning) 및 트러블슈팅 가이드라인입니다. 시스템 구동 전 반드시 아래의 설정을 적용해야 합니다.

## 1. 최적화 목표 (Objectives)
다중 센서(ROS 2)와 비전 AI 추론(ONNX Runtime)이 병렬로 실행될 때 발생하는 과도한 메모리 점유를 제어하여 OOM(Out Of Memory) 킬러에 의한 프로세스 강제 종료 현상을 방지합니다. 이를 위해 디스크 I/O 병목을 유발하는 기존 물리 스왑(Swap)을 제거하고, ZRAM(가상 압축 메모리)을 도입합니다.

## 2. 메모리 최적화 및 ZRAM 자동화 스크립트
Raspberry Pi 4의 전체 RAM(4GB) 중 **15%(약 600MB)**를 커널 최적화 기반의 ZRAM으로 할당합니다. 아래의 명령어를 한 줄로 전체 복사하여 터미널에 실행하십시오.

```bash
sudo swapoff -a && sudo rm -f /swapfile /swap.img && sudo sed -i '/swap/d' /etc/fstab && sudo apt-get update && sudo apt-get install -y zram-tools && sudo systemctl stop zramswap 2>/dev/null; sudo swapoff /dev/zram0 2>/dev/null; sudo zramctl --reset /dev/zram0 2>/dev/null; sudo rmmod zram 2>/dev/null; sudo modprobe zram && sudo sh -c 'printf "ALGO=lz4\nPERCENT=15\n" > /etc/default/zramswap' && sudo sed -i '/vm.swappiness\|vm.vfs_cache_pressure\|vm.dirty_ratio\|vm.dirty_background_ratio/d' /etc/sysctl.conf && sudo sh -c 'printf "vm.swappiness=100\nvm.vfs_cache_pressure=50\nvm.dirty_ratio=10\nvm.dirty_background_ratio=5\n" >> /etc/sysctl.conf' && sudo sysctl -p && sudo systemctl enable zramswap && sudo systemctl restart zramswap && swapon --show && free -h
```

* **정상 적용 상태 확인:** 명령어가 완료된 후, 최하단 `free -h` 출력 메시지의 `Swap:` 항목 용량이 `560Mi` 내외로 정상 가동되는지 대조하십시오. 해당 설정은 부팅 시 자동으로 활성화되도록 고정됩니다.

## 3. 하드웨어 트러블슈팅 가이드 (Troubleshooting)
자율주행 실증 테스트 중 발생할 수 있는 주요 하드웨어 동기화 이슈와 기구학적 예외 해결책입니다.

### 3.1. 모터 구동 방향 역전 (Polarity Inversion) 현상
* **증상:** 소스 코드 변경이 전혀 없음에도 불구하고 수동 조작(Teleop) 또는 AI 모드 기동 시 로봇이 후진하거나 역방향 조향을 수행하는 현상.
* **원인:** 기체 분해 조립 및 배터리 전력 차단 후 재연결 과정에서 하부 제어기(L293D 모터 드라이브 단자대 M1~M4)에 고정된 DC 하비 모터 배선의 극성이 물리적으로 반대로 체결되었을 때 발생합니다.
* **해결책:** 상위 소프트웨어 프레임워크나 브리지 코드를 임의로 수정하여 대응하지 마십시오. 역회전이 발생하는 구동륜 단자대의 물리 전선 2가닥의 체결 위치를 십자드라이버로 풀어서 **상호 반대로 교차 체결(플러스/마이너스 극성 스왑)** 하십시오.

### 3.2. 라이다(LiDAR) 자아 인지 마비 (Self-Occlusion Deadlock)
* **증상:** 가용 주행 영역 확보 및 정면 장애물 부재 상태임에도 불구하고 로봇이 긴급 제동 모드(`ai_v = 0.0`)에 강제 고정되어 진격하지 못하는 현상.
* **원인:** 라이다 센서의 360도 스캔 평면 상에 로봇 외부로 미세하게 돌출된 점퍼 와이어, 고정 프레임 구조물이 중심 반경(0.15m 이내)에 포착되어, 로봇이 자체 구조물을 정면 장애물로 오인하는 물리적 루프 현상입니다.
* **해결책:** 1. **기계적 조치:** 라이다 장착 고도(지상고 15cm) 스캔 경로 상에 어떠한 간섭물도 위치하지 않도록 배선을 타이 등으로 하단 고정하십시오.
    2. **알고리즘적 조치:** `autonomous_drive.py` 내부의 `scan_callback` 함수에서 필터링 임계치 조건(`ranges < 0.15`)이 올바르게 명시되어 있는지 점검하여 기체 자체 신호를 연산에서 배제하십시오.

#### - 이상 자세한 추가 사항은 media 폴더 안에 html 파일을 통해서 확인하십시오 -
